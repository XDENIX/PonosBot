const { createCanvas, loadImage  } = require("canvas");
const { MessageAttachment } = require("discord.js");
 module.exports = {
    name: "huita",
    aliases: [],
    cooldown: 10,
async execute(client, message, args,main) {
if(message.author.id !== '805089428690698241') return message.reply('Only dolbaeb')
 
}
};